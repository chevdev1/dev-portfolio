// 🎯 JavaScript Основы - Практические упражнения
// Каждая функция демонстрирует важные концепции JS

/**
 * 📝 Упражнение 1: Переменные и типы данных
 * 
 * В JavaScript есть 3 способа объявить переменную:
 * - var (старый способ, лучше не использовать)
 * - let (для изменяемых переменных)
 * - const (для констант)
 */
function exercise1() {
    console.log('🚀 Упражнение 1: Переменные');
    
    // Разные типы данных
    const name = "Андрей";              // Строка (string)
    let age = 25;                       // Число (number)
    let isStudent = true;               // Булево значение (boolean)
    let hobbies = ["код", "игры"];      // Массив (array)
    let person = null;                  // Пустое значение (null)
    let undefined_var;                  // Неопределённое значение (undefined)
    
    // Выводим результаты
    let result = `
📊 Типы данных в JavaScript:

1. Строка: "${name}" (тип: ${typeof name})
2. Число: ${age} (тип: ${typeof age})
3. Булево: ${isStudent} (тип: ${typeof isStudent})
4. Массив: [${hobbies.join(', ')}] (тип: ${typeof hobbies})
5. Null: ${person} (тип: ${typeof person})
6. Undefined: ${undefined_var} (тип: ${typeof undefined_var})

💡 Интересно: typeof null возвращает "object" - это историческая ошибка JS!
    `;
    
    document.getElementById('result1').innerHTML = result.replace(/\n/g, '<br>');
    console.log(result);
}

/**
 * ⚙️ Упражнение 2: Функции
 * 
 * Функции - это блоки кода, которые можно переиспользовать
 * Есть несколько способов создать функцию в JS
 */
function exercise2() {
    console.log('🚀 Упражнение 2: Функции');
    
    // 1. Обычная функция (Function Declaration)
    function greet(name) {
        return `Привет, ${name}! 👋`;
    }
    
    // 2. Функция-выражение (Function Expression)
    const multiply = function(a, b) {
        return a * b;
    };
    
    // 3. Стрелочная функция (Arrow Function) - современный способ
    const square = (x) => x * x;
    
    // 4. Функция с параметрами по умолчанию
    const createMessage = (text = "Привет", emoji = "😊") => {
        return `${text} ${emoji}`;
    };
    
    let result = `
🔧 Различные типы функций:

1. Обычная функция: ${greet("Андрей")}
2. Функция-выражение: 5 × 3 = ${multiply(5, 3)}
3. Стрелочная функция: 7² = ${square(7)}
4. С параметрами по умолчанию: ${createMessage()}
5. С кастомными параметрами: ${createMessage("Пока", "👋")}

💡 Стрелочные функции - современный и краткий способ написания функций!
    `;
    
    document.getElementById('result2').innerHTML = result.replace(/\n/g, '<br>');
    console.log(result);
}

/**
 * 📋 Упражнение 3: Массивы и циклы
 * 
 * Массивы хранят множество значений
 * Циклы позволяют обработать каждый элемент
 */
function exercise3() {
    console.log('🚀 Упражнение 3: Массивы');
    
    // Создаём массив
    const technologies = ['HTML', 'CSS', 'JavaScript', 'React', 'Node.js'];
    
    // Современные методы работы с массивами
    const upperTech = technologies.map(tech => tech.toUpperCase());
    const jsTech = technologies.filter(tech => tech.includes('Script'));
    const hasReact = technologies.includes('React');
    
    // Циклы
    let forResult = '';
    for (let i = 0; i < technologies.length; i++) {
        forResult += `${i + 1}. ${technologies[i]}\n`;
    }
    
    let forEachResult = '';
    technologies.forEach((tech, index) => {
        forEachResult += `• ${tech} (индекс: ${index})\n`;
    });
    
    let result = `
📊 Работа с массивами:

Исходный массив: [${technologies.join(', ')}]

🔄 Методы массивов:
• map() - преобразование: [${upperTech.join(', ')}]
• filter() - фильтрация: [${jsTech.join(', ')}]
• includes() - поиск: React есть в массиве? ${hasReact}

🔁 Циклы:
Обычный for:
${forResult}
forEach():
${forEachResult}

💡 Современные методы (map, filter, forEach) удобнее обычных циклов!
    `;
    
    document.getElementById('result3').innerHTML = result.replace(/\n/g, '<br>');
    console.log(result);
}

/**
 * 🏗️ Упражнение 4: Объекты
 * 
 * Объекты хранят связанные данные в виде пар ключ-значение
 */
function exercise4() {
    console.log('🚀 Упражнение 4: Объекты');
    
    // Создаём объект
    const developer = {
        name: 'Андрей',
        age: 25,
        skills: ['JavaScript', 'HTML', 'CSS'],
        isWorking: true,
        
        // Методы объекта
        introduce: function() {
            return `Привет! Я ${this.name}, мне ${this.age} лет`;
        },
        
        // Современный синтаксис метода
        getSkillsCount() {
            return this.skills.length;
        }
    };
    
    // Работа с объектом
    const newSkill = 'React';
    developer.skills.push(newSkill); // Добавляем навык
    
    // Деструктуризация - современный способ извлечения данных
    const { name, age, skills } = developer;
    
    let result = `
🏗️ Объекты в JavaScript:

👤 Информация о разработчике:
• Имя: ${developer.name}
• Возраст: ${developer.age}
• Навыки: [${developer.skills.join(', ')}]
• Работает: ${developer.isWorking ? 'Да' : 'Нет'}

🔧 Методы объекта:
• introduce(): "${developer.introduce()}"
• getSkillsCount(): ${developer.getSkillsCount()} навыков

📦 Деструктуризация:
• Извлечённое имя: ${name}
• Извлечённый возраст: ${age}
• Количество навыков: ${skills.length}

💡 Объекты идеально подходят для группировки связанных данных!
    `;
    
    document.getElementById('result4').innerHTML = result.replace(/\n/g, '<br>');
    console.log(result);
}

/**
 * 🎮 Интерактивный калькулятор
 * 
 * Демонстрирует работу с DOM, получение данных от пользователя,
 * обработку ошибок и условные операторы
 */
function calculate(operation) {
    console.log(`🔢 Выполняем операцию: ${operation}`);
    
    // Получаем значения из input полей
    const num1 = parseFloat(document.getElementById('num1').value);
    const num2 = parseFloat(document.getElementById('num2').value);
    
    // Проверяем корректность ввода
    if (isNaN(num1) || isNaN(num2)) {
        document.getElementById('calc-result').innerHTML = 
            '❌ Ошибка: Введите корректные числа!';
        return;
    }
    
    let result;
    let operationName;
    
    // Условный оператор switch для выбора операции
    switch (operation) {
        case '+':
            result = num1 + num2;
            operationName = 'сложение';
            break;
        case '-':
            result = num1 - num2;
            operationName = 'вычитание';
            break;
        case '*':
            result = num1 * num2;
            operationName = 'умножение';
            break;
        case '/':
            if (num2 === 0) {
                document.getElementById('calc-result').innerHTML = 
                    '❌ Ошибка: На ноль делить нельзя!';
                return;
            }
            result = num1 / num2;
            operationName = 'деление';
            break;
        default:
            document.getElementById('calc-result').innerHTML = 
                '❌ Неизвестная операция';
            return;
    }
    
    // Выводим результат с красивым форматированием
    const output = `
    ✅ Результат: ${num1} ${operation} ${num2} = <strong>${result}</strong>
    <br>📝 Операция: ${operationName}
    <br>🔍 Тип результата: ${typeof result}
    `;
    
    document.getElementById('calc-result').innerHTML = output;
    console.log(`Результат ${operationName}: ${result}`);
}

// 🎯 Полезные советы для изучения JavaScript:
console.log(`
🎓 СОВЕТЫ ДЛЯ ИЗУЧЕНИЯ JAVASCRIPT:

1. 🔍 Всегда используй консоль браузера (F12)
2. 📝 Пиши код руками, не копируй
3. 🧪 Экспериментируй с примерами
4. 🐛 Не бойся ошибок - они учат!
5. 📖 Читай документацию на MDN
6. 🔄 Практикуйся каждый день
7. 🚀 Создавай реальные проекты

💡 Помни: JavaScript изучается практикой, а не теорией!
`);

// Приветственное сообщение при загрузке страницы
document.addEventListener('DOMContentLoaded', () => {
    console.log('🎉 Страница загружена! Открой консоль (F12) для дополнительной информации');
});